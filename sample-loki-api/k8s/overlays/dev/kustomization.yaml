apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../deployment.yaml
  - ../../service.yaml
  - ../../ingress.yaml
patches:
  - target:
      kind: Deployment
      name: sample-loki-api
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
